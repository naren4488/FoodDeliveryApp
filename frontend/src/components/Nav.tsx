import { Button } from "./ui/button";

export const Nav = () => {
  return (
    <div>
      <Button className=" bg-red-500 font-bold text-white hover:bg-red-600 hover:text-white">
        Log In
      </Button>
    </div>
  );
};
